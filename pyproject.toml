[project]
name = "password-analyzer"
version = "1.0.0"
description = "A command-line password strength analyzer"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.scripts]
password-analyzer = "password_analyzer.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
